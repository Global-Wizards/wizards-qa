services:
  dashboard:
    build:
      context: .
    ports:
      - "8081:8080"
    environment:
      - PORT=8080
    volumes:
      - ./flows:/app/flows
      - ./reports:/app/reports
      - ./data:/app/data
